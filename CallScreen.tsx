import React from 'react';
import {Text, View} from 'react-native';

const CallScreen = () => {
  return (
    <View style={{alignItems: 'center', justifyContent: 'center', flex: 1}}>
      <Text style={{fontSize: 30}}>Call Screen</Text>
    </View>
  );
};

export default CallScreen;
